{
  "mcpServers": {
    "ozon": {
      "command": "uv",
      "args": ["run", "--directory", "${workspaceFolder}", "python", "-m", "ozon_mcp.server"],
      "env": {
        "ozon_username": "${env:ozon_username}",
        "qq_imap_auth_code": "${env:qq_imap_auth_code}"
      }
    }
  }
}
